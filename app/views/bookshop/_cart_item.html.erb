<% if cart_item == @current_item %>
  <tr id="current_item">
  <% else %>
  <tr>
  <% end %>
  <td><%= cart_item.quantity %> &times;</td>
  <td><%=h truncate(cart_item.name.gsub(/<.*?>/,''), 25) %></td>
  <td class="item-price"><%= number_to_currency(cart_item.price, :unit=> "KSh. ") %></td>
</tr>